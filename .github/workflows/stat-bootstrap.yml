# stat-bootstrap.yml — one-shot STAT setup
# Sets CLOUDFLARE_API_TOKEN in stat repo, creates KV namespace,
# updates wrangler.toml, then triggers stat deploy.

name: STAT Bootstrap

on:
  push:
    paths: ['outbox/.trigger-stat-bootstrap']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  bootstrap:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install PyNaCl + requests
        run: pip install pynacl requests --quiet

      - name: Set CLOUDFLARE_API_TOKEN in stat repo
        env:
          CF_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CROSS_PAT: ${{ secrets.CROSS_REPO_PAT }}
        run: |
          python3 << 'PYEOF'
          import base64, json, os, sys, requests
          from nacl import encoding, public

          PAT = os.environ["CROSS_PAT"]
          CF_TOKEN = os.environ["CF_TOKEN"]
          REPO = "jeffunglesbee-create/stat"
          SECRET_NAME = "CLOUDFLARE_API_TOKEN"

          r = requests.get(
            f"https://api.github.com/repos/{REPO}/actions/secrets/public-key",
            headers={"Authorization": f"token {PAT}", "Accept": "application/vnd.github+json"}
          )
          if r.status_code != 200:
            print(f"ERROR getting public key: {r.status_code} {r.text}"); sys.exit(1)
          key_data = r.json()
          print(f"Got public key for {REPO}, key_id={key_data['key_id']}")

          pub_key = public.PublicKey(key_data["key"].encode(), encoding.Base64Encoder)
          sealed = public.SealedBox(pub_key).encrypt(CF_TOKEN.encode())
          encrypted = base64.b64encode(sealed).decode()

          r2 = requests.put(
            f"https://api.github.com/repos/{REPO}/actions/secrets/{SECRET_NAME}",
            headers={"Authorization": f"token {PAT}", "Accept": "application/vnd.github+json"},
            json={"encrypted_value": encrypted, "key_id": key_data["key_id"]}
          )
          if r2.status_code in (201, 204):
            print(f"SUCCESS: {SECRET_NAME} set in {REPO}")
          else:
            print(f"ERROR: {r2.status_code} {r2.text}"); sys.exit(1)
          PYEOF

      - name: Create KV namespace in Cloudflare
        env:
          CF_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: |
          ACCOUNT_ID="b57e9af57ab46c52ca9215804e689c29"
          RESULT=$(curl -sf -X POST \
            "https://api.cloudflare.com/client/v4/accounts/${ACCOUNT_ID}/storage/kv/namespaces" \
            -H "Authorization: Bearer ${CF_TOKEN}" \
            -H "Content-Type: application/json" \
            -d '{"title":"STAT_KV"}')
          echo "Raw result: $RESULT"
          KV_ID=$(echo "$RESULT" | python3 -c "import json,sys; d=json.load(sys.stdin); print(d.get('result',{}).get('id','ERROR'))")
          echo "KV namespace ID: $KV_ID"
          echo "KV_ID=$KV_ID" >> $GITHUB_ENV

      - name: Update wrangler.toml in stat repo with KV ID
        env:
          CROSS_PAT: ${{ secrets.CROSS_REPO_PAT }}
          KV_ID: ${{ env.KV_ID }}
        run: |
          python3 << 'PYEOF'
          import base64, json, os, sys, requests

          PAT = os.environ["CROSS_PAT"]
          KV_ID = os.environ.get("KV_ID", "")
          REPO = "jeffunglesbee-create/stat"

          if not KV_ID or KV_ID == "ERROR":
            print("KV_ID not available — skipping wrangler.toml update"); sys.exit(0)

          # Get current wrangler.toml
          r = requests.get(
            f"https://api.github.com/repos/{REPO}/contents/wrangler.toml",
            headers={"Authorization": f"token {PAT}", "Accept": "application/vnd.github+json"}
          )
          if r.status_code != 200:
            print(f"ERROR getting wrangler.toml: {r.status_code}"); sys.exit(1)
          data = r.json()
          content = base64.b64decode(data["content"]).decode("utf-8")
          sha = data["sha"]

          new_content = content.replace(
            'id = "REPLACE_WITH_YOUR_KV_NAMESPACE_ID"',
            f'id = "{KV_ID}"'
          )
          if new_content == content:
            print("Placeholder not found — already updated?"); sys.exit(0)

          encoded = base64.b64encode(new_content.encode()).decode()
          r2 = requests.put(
            f"https://api.github.com/repos/{REPO}/contents/wrangler.toml",
            headers={"Authorization": f"token {PAT}", "Accept": "application/vnd.github+json"},
            json={"message": f"[STAT] Set KV namespace ID {KV_ID}", "content": encoded, "sha": sha}
          )
          if r2.status_code in (200, 201):
            print(f"SUCCESS: wrangler.toml updated with KV_ID={KV_ID}")
            print(f"Commit SHA: {r2.json().get('commit',{}).get('sha','?')[:8]}")
          else:
            print(f"ERROR: {r2.status_code} {r2.text}"); sys.exit(1)
          PYEOF

      - name: Write result
        run: |
          TS=$(date -u +%Y%m%dT%H%M%SZ)
          OUT="outbox/stat-bootstrap-result-${TS}.txt"
          echo "=== STAT Bootstrap [${TS}] ===" > "$OUT"
          echo "CLOUDFLARE_API_TOKEN: set in jeffunglesbee-create/stat" >> "$OUT"
          echo "KV_ID: ${{ env.KV_ID }}" >> "$OUT"
          echo "wrangler.toml: updated" >> "$OUT"
          echo "NEXT: stat CI will deploy on wrangler.toml push" >> "$OUT"
          cat "$OUT"
          echo "OUT=$OUT" >> $GITHUB_ENV

      - name: Push result
        run: |
          git config user.email "probe@field"
          git config user.name "STAT-Bootstrap"
          git add "$OUT"
          git diff --cached --quiet && exit 0
          git commit -m "stat-bootstrap result [skip ci]"
          for i in 1 2 3; do
            git pull --rebase --autostash origin main && git push origin main && break
            sleep $((i * 5))
          done
        env:
          OUT: ${{ env.OUT }}
