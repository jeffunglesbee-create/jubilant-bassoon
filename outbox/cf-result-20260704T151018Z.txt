=== CF API Probe [20260704T151018Z] ===
Request: GET https://jubilant-bassoon.jeffunglesbee.workers.dev/
Triggered via workflow_dispatch (mcp__github__actions_run_trigger).

Full response was the entire live-deployed index.html (31,437 lines) —
too large to store verbatim, same pattern as every prior probe this
session. Extracted only what this probe was for: confirming
CC-CMD-2026-07-04-newspaper-repaint-wipe-fix's 5 applyMainHTML
conversions are actually live.

Confirmed present (grep -n against the live response body):
  9034: applyMainHTML('<div class="empty-note">No '+(freeOnlyFilter?"free ":"")+'events found for this filter</div>');
  7285: applyMainHTML(`<div class="loading-wrap">...`);

Also confirmed zero remaining matches for
`main.innerHTML = \`<div class="(empty-note|loading-wrap)"` in the live
response — same result as the local A-NPWIPE-2 assertion, now verified
against the actual deployed bundle.

SW_VERSION in live response: '2026-07-04e' — matches commit 2c3f604
(the newspaper-repaint-wipe-fix commit), confirming this exact commit
is deployed.

HTTP 200, Content-Type: text/html.
