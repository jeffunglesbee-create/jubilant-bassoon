# MISFILED — see field-relay-nba instead

This CC-CMD was accidentally committed to this repo (jubilant-bassoon) due to a
tool-default mistake — the `repo` param wasn't specified and defaulted here.

The real, correct copy lives at:
`field-relay-nba/docs/CC-CMD-2026-07-12-drama-peak-nightstars-invalidation.md`
(commit `0cda27d`)

Everything in this CC-CMD targets `field-relay-nba` exclusively
(`scripts/drama-backfill.mjs`, `src/analytics-engine.js`) — nothing in it
applies to this repo. Do not dispatch Claude Code against this file.

Left as a short redirect rather than silently overwritten with unrelated
content, so anyone who finds this by the old path/commit history sees the
correction, not a mystery.
