# CC Session Outbox — Home Run Derby entry (v2, supersedes v1) (CC-CMD-2026-07-13-hrd-entry-v2)

**Date:** 2026-07-13
**Scope:** one entry in `index.html`'s static schedule-card array.

## TASK 0 — Probe

**Confirmed `docs/CC-CMD-2026-07-13-hrd-entry.md` (v1) never executed:** `git log --oneline --all -- docs/CC-CMD-2026-07-13-hrd-entry.md` shows exactly one commit (`de3c142`), and `git show --stat` on it shows the commit only added the spec doc itself (`docs/CC-CMD-2026-07-13-hrd-entry.md | 53 +++...`, zero `index.html` changes) — the same misleading-commit-title pattern confirmed repeatedly this session (a commit message describing real work that the diff shows never actually happened). Directly grepped `index.html` for "Home Run Derby" / "T-Mobile Home Run Derby" — found only generic references (a Netflix streaming-bundle description, an unrelated programming-note teaser, and this session's own earlier `hrd-bracket-client` work) — no static schedule-card entry existed. No duplicate risk; wrote a fresh entry.

**Real insertion point and card shape, found fresh (the CC-CMD's suggested `startDate:"2026-07` grep pattern returned nothing useful — file has drifted since that estimate was written):** `MEDIA_SPECIALS` (~L13025), a "Date-windowed one-off events" array of `{ startDate, endDate, card:{ show, network, chip, time, desc, journalNote, link } }` objects, consumed by `renderMedia()`'s `todayStr >= s.startDate && todayStr <= s.endDate` filter (~L13682) — confirmed exactly matching the shape TASK 1 asked for. Read several neighboring real entries (PL Final Day, WC Final, NHL/NBA pregame cards) to match tone, length, and field conventions exactly.

**`netflix` chip color:** confirmed absent from `MEDIA_CHIP_COLORS` (~L13716) as the CC-CMD anticipated — flagging per its own instruction, and also fixing it (a 1-line, zero-risk addition using Netflix's real brand color `#e50914`, already used elsewhere in this file for the Netflix `STREAMING_APPS` entry at ~L6094 — reused, not invented).

## TASK 1 — Entry written

Added to `MEDIA_SPECIALS`, `startDate`/`endDate` both `"2026-07-13"` (today only — the CC-CMD's CONTEXT explicitly warns the MLB All-Star Game is the *following* night and must not be conflated; verified via forced test below that the entry is correctly absent on 2026-07-14).

- `show`: `"T-Mobile Home Run Derby"`
- `network`: `"Netflix"`, `chip`: `"netflix"` (new chip color added, see TASK 0)
- `time`: `"8:00 PM ET · Citizens Bank Park, Philadelphia"`
- `desc`: all 8 players with their real teams and real stakes woven in verbatim from the CC-CMD's own verified facts — Schwarber's HR lead, Harper's home-park/2018-title/2013-runner-up history, Caminero's 2025 runner-up return (to Cal Raleigh), Murakami joining Ohtani as the only Japanese-born participants, plus Rice/Caglianone(youngest at 23)/Contreras/Walker(first-time All-Star), the swing-based-format change replacing the timed clock since 2015, and the 7 PM ET pre-show. Nothing invented beyond what the CC-CMD supplied.
- `journalNote`: tighter version, leads with the Netflix-exclusivity angle (the CC-CMD's own suggested "single most FIELD-relevant angle... the genuinely novel story tonight").
- `link`: `https://www.netflix.com/browse/genre/81545700` — the same real, already-in-file Netflix MLB URL used at ~L6094, not a newly-invented one.

## TASK 2 — Verify

- `node smoke.js index.html`: 920 passed, 0 failed.
- `node field_unit.js`: 66 passed, 0 failed (not required by this CC-CMD, run anyway per this session's standing discipline for any `index.html` change).
- Syntax: full-file `new Function()` parse of every `<script>` block — clean.
- **Real forced test, not visual inspection:** extracted the live `MEDIA_SPECIALS` array from the committed file and ran it through the exact filter logic `renderMedia()` uses.
  - Exactly 1 entry with "Home Run Derby" in its `show` field — confirmed no duplicate.
  - Active on `2026-07-13` → `['T-Mobile Home Run Derby']` (real, correct).
  - Active on `2026-07-14` (All-Star Game night) → `[]` — confirmed the entry does NOT bleed into the following night, matching the CC-CMD's explicit "don't conflate" warning.
  - Active on `2026-07-12` (day before) → `[]` — confirmed no premature appearance either.
- `MEDIA_CHIP_COLORS.netflix === "#e50914"` confirmed present in the committed file (not just the diff) — the chip resolves to the real Netflix brand color, not the `default` gray fallback.

## DONE CONDITION

One accurate, richly-contextualized Home Run Derby entry (not a placeholder) — confirmed via real forced test to render correctly for today (2026-07-13) and stay absent on the surrounding dates. No duplicate (v1 confirmed never landed). Smoke clean.

## Confidence score

- TASK 0 correctly determines v1 never landed (verified via `git log` + `git show --stat`, not assumed), avoids duplication, finds the real current insertion point and card shape fresh since the CC-CMD's own line estimate was stale: 25/25
- TASK 1 factually accurate on all 8 names/teams/context (every name, team, and stated fact traced directly to the CC-CMD's own verified CONTEXT, nothing invented), matches the established `MEDIA_SPECIALS` card format exactly (confirmed against multiple real neighboring entries): 45/45
- TASK 2 clean and confirmed via real forced-condition testing against the actual filter logic (not just "smoke passed"): active on the right date, absent on the surrounding dates, no duplicate, chip color resolves correctly: 30/30

**Total: 100/100.**

## Commit

- `index.html`: added the `T-Mobile Home Run Derby` entry to `MEDIA_SPECIALS`, added `netflix:"#e50914"` to `MEDIA_CHIP_COLORS`. `SW_VERSION` bumped `2026-07-13g` → `2026-07-13h` (deploy-trigger-path change, standing discipline).
- `sw.js`: `SW_VERSION` synced.
- This manifest.

**Note carried forward from the CC-CMD's own CONTEXT** (not this session's finding, quoting it for visibility): this is a recurring annual event that eventually deserves real "tournament brief" treatment matching the golf pattern (fetch → leaderboard render → journalism context) rather than a permanent static card — explicitly out of scope for this CC-CMD, a stopgap for tonight only. This session's earlier, separately-dispatched `hrd-bracket-client` work (bracket structure + journalism context functions) is the first piece of that eventual richer treatment, but is not wired to this static entry or to any live data source yet.
