# CC Session — esbuild Phase 2b (source-of-truth inversion)
**Date:** 2026-07-18
**Repo:** jeffunglesbee-create/jubilant-bassoon
**Branch:** main

## HEAD Progression
- Start: `eeedd3e` (docs: relocate esbuild-phase2b-corrected CC-CMD)
- End: `b4bf1ee` (ci: deploy-gate SW_VERSION sync — target src/legacy/field.js, sync → index.html)

## Smoke
- Start: 958/0
- End: 958/0 (verified after all changes, sync dry-run, and revert)

## SW_VERSION
- Not bumped (no deploy-triggering commit to index.html/sw.js)

## Commits This Session (4)

| SHA | Message |
|-----|---------|
| `870cf50` | feat: add scripts/sync-source.mjs — propagate src/legacy/field.js to index.html [skip ci] |
| `549892e` | fix: pre-commit — sync src/legacy/field.js → index.html before smoke [skip ci] |
| `25b0c20` | fix: build-bundle.mjs — add GENERATED comment to injected script block [skip ci] |
| `b4bf1ee` | ci: deploy-gate SW_VERSION sync — target src/legacy/field.js, sync → index.html |

## What Was Built

**Phase 2b: src/legacy/field.js is now the genuine edit target.**

### scripts/sync-source.mjs (new)
Reads `src/legacy/field.js`, writes it into `index.html`'s main script block using `lastIndexOf('<script>')` with 1MB sanity-check guard. This is the core propagation script.

### scripts/pre-commit (updated)
Added sync step immediately before smoke: `node scripts/sync-source.mjs && git add index.html`. This ensures that whenever `src/legacy/field.js` is edited and committed, the pre-commit hook propagates it to `index.html` before running smoke, and stages the updated `index.html` automatically.

### scripts/build-bundle.mjs (updated)
Added GENERATED comment to the injected script block header:
```
// GENERATED BY build-bundle.mjs — edit src/legacy/field.js, not this block
```
Any future session opening `index.html` directly sees an immediate, unambiguous warning not to edit it there.

### .github/workflows/deploy-gate.yml (updated)
SW_VERSION sync step now targets `src/legacy/field.js` instead of `index.html` directly, then runs `sync-source.mjs` to propagate before smoke. Commit-back diff scope expanded to include `src/legacy/field.js`.

## Dry-Run Verification (TASK 6)
- Added trivial comment to `src/legacy/field.js`
- `node scripts/sync-source.mjs` → confirmed edit appeared in `index.html`
- `node smoke.js index.html` → 958/0
- Reverted trivial edit, re-synced, re-smoked → 958/0

## Integration Status
**VERIFIED** — end-to-end local dry-run proven. Smoke stays against `index.html` (not `src/legacy/field.js` directly) because smoke has 102 HTML/CSS assertions that fail against a JS-only file. sync-source.mjs is the bridge.

## Trigger Path Reasoning (TASK 5)
Editing `src/legacy/field.js` → pre-commit hook syncs to `index.html` and stages it → `index.html` appears in every commit diff → deploy-gate.yml fires. No change to trigger paths needed.

## Live CI Verification (TASK 5 of original CC-CMD / pending)
No deploy-gate.yml run was triggered by these 4 commits (none touched trigger paths). The new pipeline will execute on the next real code change to `index.html`, `sw.js`, `field_utils.js`, or `wrangler.jsonc`. The next session making such a change should verify the actual job/step logs confirm the new SW_VERSION sync step targets `src/legacy/field.js`.

## What Was NOT Built (Scope Boundary)
- Smoke.js target not changed — stays against `index.html` (by design)
- `src/legacy/field.js` trigger path not added to deploy-gate.yml — not needed (index.html appears in diff via sync)
- No changes to any application code
