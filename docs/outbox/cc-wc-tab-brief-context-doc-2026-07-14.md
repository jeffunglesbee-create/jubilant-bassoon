# CC Session Outbox — Document wc-tab-brief's deliberate context exclusion (CC-CMD-2026-07-14-wc-tab-brief-context-doc)

**Date:** 2026-07-14
**Scope:** one comment added above `wc-tab-brief`'s enqueue call site. Zero behavior change.

## TASK 0 — Probe

`grep -n "briefType:\s*'wc-tab-brief'" index.html` → single real match, L34291 (inside `fetchWCTabBrief(wcGames)`, ~L34247). Read the full call site fresh: `gameLines = wcGames.slice(0, 4).map(...)` builds a multi-game summary (up to 4 matches, team names/kickoff/network/league only) baked into the `prompt` string. The `body: JSON.stringify({...})` sent to `JOURNALISM_ENQUEUE_RELAY` carries `prompt`/`sport`/`briefType`/`max_tokens`/`scoreThreshold` — no `home`/`away`/`matchupNote` fields, confirmed still absent. The CC-CMD's premise holds: this is still genuinely a multi-game slate summary, not narrowed to a single game since the doc was written.

## TASK 1 — Comment added

Read the real sibling comments first to match tone/specificity exactly:
- Finals Desk (L35778-35791): explains *why* `home`/`away`/`homeScore`/`awayScore`/`matchupNote` were added, references `CC-CMD-2026-07-09-finals-desk-context-gap` by name, cross-references night-owl/scouts-pick as the same bug pattern.
- Night Owl (L40619-40624): same structure, references `CC-CMD-2026-07-09-enqueue-context-gap` by name.

Added a comment of the same shape immediately above `body: JSON.stringify({...})` at the real call site (L34285-34297), stating the mirror-image reasoning — why these fields are deliberately *absent* here, not omitted by oversight:

```
// Deliberately no home/away/matchupNote (CC-CMD-2026-07-14-wc-tab-
// brief-context-doc): unlike scouts-pick/finals-desk/night-owl --
// each a single-game brief, fixed this month for the same enqueue-
// context-gap bug -- wc-tab-brief summarizes up to 4 games in one
// prompt (gameLines above), so there's no single game to anchor
// Context Anchoring/Matchup Depth to. Same shape as the relay's
// own cron-slate path, which passes sport: null with no single-
// game context for the identical reason. Not an oversight.
```

References this CC-CMD by name, matches the sibling comments' convention of naming the specific dimension gap (Context Anchoring/Matchup Depth) and citing the relay's own precedent (`cron-slate`) for the same deliberate-exclusion shape. Zero behavior change — comment only, no line inside the `body:` object touched.

## TASK 2 — Verify

- Syntax: full-file script-block parse (`new Function()` over every `<script>` block) — 2/2 blocks parsed clean, 0 failures.
- `node smoke.js index.html`: 924 passed, 0 failed — identical count to pre-change (no regression, no new assertion expected since this is comment-only).
- `git diff -- index.html`: exactly one hunk, 8 added lines (the comment), 0 removed lines, 0 lines changed inside the `body:` object or anywhere else in the file. Confirms zero functional change.

## DONE CONDITION

The exclusion is now explicitly documented at the real call site with real, specific reasoning (multi-game slate shape, no single game to anchor Dimensions 7/10 to, matches the relay's own `cron-slate` precedent) — matching the existing convention used by the three sibling call sites that took the opposite fix. Zero behavior change, confirmed via diff (exactly 8 comment lines added) and smoke (924/924, unchanged).

## "Automate follow-ups. No fallbacks, only fixes." (per this dispatch's own instruction)

- No follow-up work generated by this task — it's a terminal documentation fix, not a staged/partial change. Nothing to automate.
- No fallback-shaped code introduced or reviewed here — this task added a comment only, no runtime logic.

## Confidence score

- TASK 0 confirmed the call site is still genuinely multi-game (real grep + real read, not assumed): 30/30
- TASK 1 comment added, matches the real sibling-comment convention (read Finals Desk and Night Owl's actual comments first, not guessed), zero functional change: 40/40
- TASK 2 real smoke/diff verification: 924/924 unchanged, diff shows exactly the 8-line comment: 30/30

**Total: 100/100.**

## Commit

- `index.html`: one comment block added above `wc-tab-brief`'s enqueue `body:` at L34285. No functional change.
- This manifest.
