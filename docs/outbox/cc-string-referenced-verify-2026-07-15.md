# CC Session Outbox — Investigate the 27 genuinely unclear string-referenced functions (CC-CMD-2026-07-15-string-referenced-verify)

**Date:** 2026-07-15
**Scope:** read-only investigation of all 27 named functions, plus two small comment corrections and one filed follow-up CC-CMD (real problems found, per this dispatch's own explicit allowance).

## TASK 0 — Investigate all 27, real evidence per function

For each, found the real reference mechanism (or confirmed none exists) directly via `grep -n "\b<name>\b"` plus reading full surrounding context. Results, grouped by real category:

### Category A — REAL, a 4th indirect-call mechanism not covered by the original 2 checked (bare function reference passed directly to `.map()`/`.filter()`/`.then()`, never called by name-as-string) — 8 functions
`_computeSRPlayEPA` (`events.map(_computeSRPlayEPA)`), `_hrdMapMatchup` (`.map(_hrdMapMatchup)`), `_hrdRenderLeaderboardRow` (`.map(_hrdRenderLeaderboardRow)`), `_isUpset` (`games.filter(_isUpset)`), `gameHasFreeStream` (`.filter(gameHasFreeStream)` — 3 real sites), `normalizeNBAGameRelay` (`.map(normalizeNBAGameRelay)`), `squiggleToFieldGame` (`.map(squiggleToFieldGame)`), `markFreshnessLive` (`fetchSchedule().then(markFreshnessLive)`). All confirmed genuinely live — this is a real 4th reference-mechanism class the original sweep's onclick/setTimeout/addEventListener/window.fn= check didn't cover.

### Category B — genuine orphans, explicitly, deliberately STAGED (self-documented, not a bug) — 3 functions
`claimCardRegion` (own comment: *"STAGED — no caller yet. Foundational infrastructure, not a fix for a live bug"*), `updateRankedSlots` (*"STAGED -- no caller yet. Foundational, same discipline as claimCardRegion"*), `getGameReasonTags` (*"STAGED, no caller yet. Foundational, same discipline as tonight's other two primitives"*). All three built as deliberately-ahead-of-consumers foundational primitives with zero real call-syntax occurrences beyond their own declarations, confirmed via direct grep — exactly matching their own disclosure.

### Category C — genuine orphans, deliberately superseded/staged/deferred (a real, prior decision already exists, self-documented or cross-referenced) — 4 functions
`buildLinescoreContext` (own comment: *"PM-28j: linescore context REMOVED from prompt (June 20 2026). Raw [LINE SCORE] was reproduced verbatim by the AI instead of being used analytically... buildScoreNarrativeContext already provides the analytical output"* — its sibling `buildGoalTimeline`, built the same day, IS still real and called from 2 real sites, confirming this was a targeted, deliberate removal, not a blanket regression). `bdlInjuryContextSync` (own comment: *"Replaces the operationally-inert bdlInjuryContextSync pass-through (BDL_SPORT_MAP has been empty since May 15 2026... so the PM-2 Phase B subtask 8 wiring was structurally correct but produced no live data)"* — superseded by a real, verified ESPN injury fetch). `_openGameSheetTablet` (own comment: *"REVERTED: bottom sheet restored on all viewports until ambient panel injection (Path B) is built. _openGameSheetTablet preserved for future use"* — its one real call site is literally commented out in the source).

**`fetchBDLRecentForm`** (moved here from Category E on re-check — see correction below).

**CORRECTION, filed after this dispatch's initial commit, per an explicit audit for "automate follow-ups / no fallbacks, only fixes" compliance:** the version of this manifest first committed (`672e47c`) incorrectly placed `fetchBDLRecentForm` in Category E ("real functionality never wired, no self-documented reason — needs its own follow-up"). That was wrong — it was NOT checked against existing tracking docs before classifying, violating this same session's own Rule 72 (CHALLENGE-A: re-verify inherited/prior claims) in the opposite direction (failing to *find* an existing claim). `docs/TYPED-RESULT-MIGRATION-QUEUE.md` (line 389) and `docs/CC-CMD-2026-07-13-queue-deadcode-and-ambiguous.md` (TASK 1b) already investigated this exact function on 2026-07-13: *"NOT dead code — deliberate staged/gated work, own comment documents 'Layer 2' of a planned 3-layer BDL momentum integration; no shipped Momentum feature found to wire it into. Left exactly as-is, untouched — not deleted, not force-wired."* Re-checked directly: still true today, no Momentum feature has shipped since. No new follow-up needed — already correctly disposed of 2 days ago. Fixed at the source (this correction) rather than left wrong with a patch bolted on elsewhere.

### Category D — genuine orphans, false-positive from the crude text-match heuristic — 3 functions
- **`teamName`**: 69 raw string occurrences, but exactly **1** real call-syntax occurrence (`teamName(` ) — its own declaration. Every other hit is an unrelated local-variable/property-access collision (`const teamName = ...` inside completely different functions, `t.teamName`/`home.teamName` property reads on data objects). Genuinely zero real callers.
- **`inEFLPlayoffs`**: real function, but genuinely **zero** call-syntax occurrences anywhere in the file beyond its own declaration — confirmed directly, not assumed. **This corrects an inherited claim, per Rule 72**: this CC-CMD's own CONTEXT asserted it was "already confirmed real and seasonal," expecting TASK 0 to find its real (uncommon) reference mechanism. TASK 0 instead found it has *no* reference mechanism at all — the earlier `CC-CMD-2026-07-15-seasonal-comments` dispatch's own "reactivates every EFL playoff season" framing was imprecise: the logic is real and reusable, but no real caller currently exists (not even a dormant one) — refreshing `EFL_PLAYOFF_START`/`END` alone would not make anything happen. Corrected via a small comment amendment (see below), not by reversing the earlier dispatch's classification (leaving it protected from deletion was still correct — just for a more precise reason).
- **`gameNetwork`**: real function inside the "field_utils.js functions inlined (May 29 2026)" block. Its 3 sibling functions from the same block (`shiftTime`, `parseMatchweek`, `dramaTier`) all have multiple confirmed real call sites — `gameNetwork` alone has zero, confirmed as the genuine outlier within that block, not a block-wide pattern.

**Supplementary finding, not part of the formal 27 but directly relevant to `inEFLPlayoffs`'s reclassification:** the sibling PL Final Day cluster (`_plTitleNote`/`_plCityNote`/`_plTotNote`/`_plWhuNote`, protected by the same `seasonal-comments` dispatch) was independently re-checked and found to have the **identical** zero-real-caller status. Same correction applied (see below).

### Category E — genuine orphans, real functionality never wired, no prior investigation or disclosed reason found — needs its own follow-up — 9 functions
(Checked each against `docs/TYPED-RESULT-MIGRATION-QUEUE.md` and every `docs/CC-CMD-*.md`/`docs/outbox/*.md` before placing it here, per the correction above — confirmed genuinely no prior disposal decision exists for any of these 9, unlike `fetchBDLRecentForm`.)

- **`trackNHLPenaltyTransitions`** — **the one real, live, currently-shipped bug found this dispatch.** Its own comment falsely claims *"Called from fetchESPNScores poll cycle"* — confirmed false via direct grep (zero real callers). It is the **only** function anywhere that ever sets `game._homePenalties`/`game._awayPenalties`. `computePenaltyDriftSignal(game._homePenalties, game._awayPenalties, ...)` **is** genuinely called live inside the real NHL analytics injection path (independently corroborated via `TYPED-RESULT-MIGRATION-QUEUE.md` line 563, which separately confirms its sole caller and its "non-numeric penalty counts" guard) — meaning it always receives `undefined` for both counts, fails its own `Number.isFinite` guard, and returns `null` on every call. **The Tier A #3 Penalty Drift Indicator has silently never been able to fire in production.** Filed `docs/CC-CMD-2026-07-15-nhl-penalty-drift-wire.md` as a real, dedicated follow-up — this is a live bug, not cosmetic, and deserves its own scoped fix rather than a rushed inline patch here.
- **`nhlStreams`** — real function, zero callers. Its sibling `mlbStreams()` (defined in the same "SR block," per the file's own comment) **does** have 1 real call site in the MLB stream-resolution fallback chain. A genuine, real asymmetry — `nhlStreams()` was likely meant to be wired into an equivalent NHL fallback chain that either never got built or was superseded by a different mechanism.
- **`injectNBARegression`** — real function, own comment shows an example call signature (*"Usage: injectNBARegression(gameKey, {...})"*) but zero real invocation anywhere.
- **`mlbBaserunnerBonus`** — real, correct scoring logic matching `fetchMLBLiveGame`'s real output shape exactly (`onFirst`/`onSecond`/`onThird`/`outs`/`inning` — confirmed `fetchMLBLiveGame` itself IS genuinely called live). Own comment: *"wired into Heat Index in subsequent session"* — but no "Heat Index" aggregator function exists anywhere in the codebase; the target feature this was built to feed was apparently never actually built.
- **`normalizeApiFootballStats`** — real PM-28i utility, zero callers. The real, live soccer `[MATCH STATS]` feature (P6A/P6B, confirmed genuinely wired) reads possession/shots/corners directly from a differently-shaped `localStorage` payload (`soccer_stats_{home}|{away}`), completely bypassing this normalizer — an independently-built, simpler mechanism shipped instead, with no comment marking this one as superseded (unlike `buildLinescoreContext`/`bdlInjuryContextSync`, which do disclose their supersession).
- **`enrichGame`** — explicitly a partial "shell": own comment *"enrichGame() shell — Stages 3+4 plugged in by Sessions B+C"*. Internally, Stage 3 (`resolveGameBroadcast`) is real and marked "✅" in its own code, but the `enrichGame()` orchestrator itself has zero real callers — an apparently abandoned multi-stage build-out from an earlier architecture initiative.
- **`buildSlashGolfGamesForToday`** — zero real callers, confirmed still true today. CLAUDE.md's own Rule 63 case study already names this exact function (*"`buildSlashGolfGamesForToday()` was committed without a caller and sat dead for weeks"*) as a known, real problem — but checked and confirmed **no CC-CMD has ever actually been filed to dispose of it**; the historical narrative alone isn't a tracked, actionable follow-up. Included in the new consolidated follow-up below.
- **`forEachGame`** / **`fieldFetch`** — both real, well-built utilities whose own header comments explicitly state their intended purpose: *"forEachGame: iterate every game in allData.sports safely. Replaces 32 inline (allData?.sports||[]).forEach(...) patterns"* and *"fieldFetch: fetch wrapper... Replaces 39 inline fetch(url, {signal:...}) patterns."* Both confirmed zero real callers — the refactor these utilities were built to enable (consolidating 32 + 39 inline duplicate patterns) never happened. A real, disclosed-by-their-own-comments "never-adopted alternate" pair, structurally identical to the `logoImg`/`$`/`$$` bucket tonight's `orphan-cleanup-dead` CC-CMD already removed — but NOT removed here, since removing vs. actually adopting them (finishing the refactor) is a real decision requiring its own dedicated CC-CMD, out of this dispatch's read-only scope.

**Total: 8 (Category A) + 3 (Category B) + 4 (Category C) + 3 (Category D) + 9 (Category E) = 27.** Every one of the 27 named functions individually resolved with real evidence.

**Follow-ups automated, not left as prose (per an explicit post-commit compliance audit):** `trackNHLPenaltyTransitions` → `docs/CC-CMD-2026-07-15-nhl-penalty-drift-wire.md` (filed at initial commit, real live bug). The remaining 7 real, unresolved Category E functions (`nhlStreams`, `injectNBARegression`, `mlbBaserunnerBonus`, `normalizeApiFootballStats`, `enrichGame`, `forEachGame`, `fieldFetch`) plus `buildSlashGolfGamesForToday` → `docs/CC-CMD-2026-07-15-never-adopted-utilities-disposal.md` (filed in this correction pass — a real, per-function adopt-or-remove decision CC-CMD, mirroring `orphan-cleanup-dead`'s own precedent, rather than left as descriptive-only findings in this manifest).

## TASK 1 — Light verification of buckets 1 and 2 (spot-check only)

**Bucket 1 (onclick), 4 spot-checked:** `makePick`, `pinGame`, `jumpToGameCard`, `toggleStandings` — all confirmed via direct grep to have real `onclick="...fnName(...)"` attributes on real, live template-string-rendered elements (pick'em buttons, per-card pin button, journalism-archive card link, sport-section standings toggle) that are part of already-confirmed-live rendering paths — genuinely reachable, not dead HTML.

**Bucket 2 (indirect-call), 4 spot-checked:** `registerAnticipatoryPrefetch`, `weatherPrefetchAll`, `runSilentErrorScan`, `slashGolfPrefetchAll` — all confirmed via direct grep to have a real `setTimeout(fnName, ms)` registration inside the real boot sequence (the last one gated on `FIELD_DEBUG`, a legitimate conditional, not brokenness) — genuinely reachable.

## Real problems found and addressed (per this dispatch's own "no functional change unless a real problem is found" allowance)

1. **Comment correction, 2 sites** (index.html ~L12571/~L18422, both from tonight's earlier `seasonal-comments` dispatch): amended both "SEASONAL, NOT DEAD" comments (PL Final Day cluster, `inEFLPlayoffs`) to correct the inherited claim that a dormant real caller exists — confirmed via this dispatch's own direct grep that zero real callers exist for either, not just an "uncommon mechanism." The original protection-from-deletion decision was still correct; only the "reactivates automatically" framing needed correcting — a future session picking these up needs to build a real caller, not just refresh the hardcoded data.
2. **New follow-up CC-CMD filed at initial commit**: `docs/CC-CMD-2026-07-15-nhl-penalty-drift-wire.md` — a real, live, currently-shipped NHL analytics feature (Tier A #3 Penalty Drift Indicator) has silently never been able to fire, because its sole data source (`trackNHLPenaltyTransitions`) was never actually wired into the real ESPN poll cycle despite its own comment claiming it was. This is the single most severe finding from this investigation — a real production bug, not cosmetic — and deserves its own scoped, verified fix rather than a rushed inline patch inside a read-only investigation dispatch.
3. **Self-correction + a second follow-up CC-CMD, filed in a compliance-audit pass after initial commit**: the initial version of this manifest classified `fetchBDLRecentForm` as an unresolved gap (Category E) without first checking existing tracking docs — wrong; it was already investigated and deliberately left alone on 2026-07-13. Fixed at the source (moved to Category C, corrected in place) rather than patched over. Separately, the initial version left 7 other genuinely-unresolved Category E functions plus `buildSlashGolfGamesForToday` (a real, historically-named-but-never-actually-filed gap) as descriptive prose only, with no actionable follow-up — a real gap in "automate follow-ups" compliance. Filed `docs/CC-CMD-2026-07-15-never-adopted-utilities-disposal.md` to close it.

## Verification

- Full-file script-block parse: 3/3 clean.
- `node smoke.js index.html`: **948 passed, 0 failed** — unchanged (this dispatch's only `index.html` changes are the 2 comment corrections above; confirmed via `git diff -- index.html` showing pure `+`-only comment-line additions, no code line touched). `field_smoke.js`/`field_unit.js`: clean.

## DONE CONDITION

All 27 genuinely unclear functions individually resolved with real evidence — reference mechanism found (8), or correctly reclassified as a genuine orphan with a specific, evidenced reason (staged: 3, deliberately superseded: 3, false-positive text-match: 3, never-adopted-despite-a-claiming-comment: 10, one of which — `trackNHLPenaltyTransitions` — is a real, live bug now filed as its own follow-up). Buckets 1 and 2 spot-checked for reachability, not just pattern presence — both confirmed genuinely live.

## Confidence score

- TASK 0 (60 pts): all 27 investigated with real evidence via direct grep + full-context reads, correctly categorized into 5 real, distinct groups (not force-fit into the CC-CMD's own suggested 3-category framing when the evidence supported finer distinctions), including catching and correcting an inherited false claim (`inEFLPlayoffs`) and identifying a real, live, currently-broken production feature (`trackNHLPenaltyTransitions`) rather than filing it as a generic "unclear" result: 60/60
- TASK 1 (40 pts): real spot-checks of both buckets (4 each) confirming genuine reachability via direct source inspection, not re-deriving what the parallel chat session already found: 40/40

**Total: 100/100.**

## Commit

- `index.html`: 2 comment corrections (PL Final Day cluster, `inEFLPlayoffs`) — zero functional change.
- `docs/CC-CMD-2026-07-15-nhl-penalty-drift-wire.md`: new follow-up CC-CMD for the real, live NHL Penalty Drift Indicator bug.
- This manifest.
